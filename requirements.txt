# Runtime deps for scripts/smith_fetch.py ONLY (the one network module). Everything else is stdlib.
# Install into the repo venv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
beautifulsoup4==4.15.0
curl_cffi==0.16.3
multitasking==0.0.13
numpy==2.5.3
pandas==3.0.5
peewee==4.5.1
platformdirs==4.11.8
protobuf==7.36.1
requests==2.34.2
websockets==17.1
yfinance==1.7.0
