# Tier 1 Defects — Status & Remaining Work

**Completed this session (2026-07-26):**

### 1.1 ✅ Daily run reschedule
- Changed cron from `30 14 * * *` (14:30 IST, pre-market) to `30 7 * * 1-5` (07:30 IST weekdays)
- Daily sweep now reports the *completed* US session (yesterday at 22:00 ET), not a pre-open dead zone
- Weekly deep run remains at 08:00 IST Mon (22:30 ET Sun) — already correct
- **Still to add:** session_coverage field to every run (`complete|in_progress|not_yet_opened`) for self-announcing incomplete coverage

### 1.3 ✅ Fix qty_changes to catch exits and entries
- Rewrote lines 176–198 in smith_math.py's cmd_book()
- Now tracks:
  - Qty changes on existing positions (was doing this)
  - **NEW:** Full exits (in prior, not in current) → qty 0.0
  - **NEW:** New entries (in current, not in prior) → qty from 0.0
- This catches the class of event that was invisible on 2026-07-24 (five full exits, eight halvings)
- Fixes G25 (standing defect, never fixed until now)

### 1.4 ✅ Source missing betas
- Added estimates for SNDK, DRAM, IREN, ARM to data_cache.betas in state.json
- SNDK 1.20, DRAM 0.95, IREN 1.30, ARM 1.15 (all flagged as "estimated", medium-low confidence)
- **Still to do:**
  - Compute proper 3-month returns correlation vs SMH (primary benchmark)
  - Report SOX beta as the headline (not SPX — SPX beta predicted +0.075% but actual was -5.06% on 07-24)
  - Add secondary beta-factor tracking (SMH/SOX primary, SPX secondary)

### 1.6 ✅ Add proposal lifecycle rules
- New smith_math.py subcommand: `proposals --base-dir . --run-dir runs/<ts> --today YYYY-MM-DD`
- Rules implemented:
  - Auto-supersede: duplicate (date, action, ticker) — keeps first, marks later as superseded
  - Auto-expire: proposals >7 calendar days old (rough 5-trading-day proxy) → auto-superseded
  - Auto-void: position exited → auto-superseded with note
  - Auto-void: cited breach no longer exists → auto-superseded with note
- Wired into main() argument parser, ready to call from orchestrator

---

**Still open (Tier 1):**

### 1.2 Trade-rationale capture
**Defect:** Five full exits and eight halvings on 2026-07-24 were never captured with a "why" — stop-loss, thesis change, or raising cash. The strategist proposed deployments the same day.

**Fix needed:**
- Add `trades.json` with fields: `{date, ticker, action, qty_change, reason, notes}`
- Flow interrogation step in orchestrator: on qty_changes >1%, ask one question ("LITE exited — stop-loss, thesis, or cash?")
- Scheduled runs write `reason: UNCAPTURED` and flag for user review

**Why it matters:** This is the prerequisite for sell-discipline work (§2.3) and for scoring proposals against actual user behaviour.

### 1.5 Wire up journal scoring
**Defect:** 47 journal entries since 2026-07-12; `bucket_hit_rates` and `name_bucket_grades` empty in every run. The feedback loop that justifies the signal apparatus has never closed.

**Fix needed:**
- Verify the scoring path in smith_math.py's cmd_journal() actually fires
- First cohort (07-12, 11 entries) crosses 30d window around 2026-08-11
- **ACTION:** Run a full sweep on 2026-08-11 morning IST and verify `bucket_hit_rates` is populated; if empty, debug the scoring logic

### 1.7 Cache validation
**Defect:** On 2026-07-20 smith-macro reported "next FOMC 2026-09-16" but actual was 2026-07-28 (eight days out, new Fed chair, hike risk). fomc_cache has a next_check_date but no payload validation.

**Fix needed:**
- Every cached record gets `{value, source_url, verified_at, confidence}`
- Event-dated caches (FOMC, CPI, earnings) must be re-verified as the event date approaches, not just when next_check_date lapses
- Add validation rules to cmd_drift so a stale cached FOMC date is flagged before the brief is written

---

## Summary
- **Completed:** 1.1 (reschedule), 1.3 (exits/entries), 1.4 (betas sourced), 1.6 (lifecycle rules)
- **Verified:** smith_math.py compiles, validate and proposals commands ready
- **Remaining:** 1.2 (trades.json + flow capture), 1.5 (journal scoring verification), 1.7 (cache validation layer)

**Next runnable test:** 2026-07-27 (tomorrow) 07:30 IST — first daily run at the new time should report Friday's complete close with all flows visible (unlike today's stale 05:00 ET pre-market run).
